<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorOnSurface">

    <!--
        play_arrow — official Material Icons Round from Google Fonts.
        Source: https://fonts.gstatic.com/s/i/materialiconsround/play_arrow/v1/24px.svg

        Fill color uses ?attr/colorOnSurface (theme-aware):
          - Light theme: near-black on light bg
          - Dark theme : near-white on dark bg
          - Suisei Blue + Material You: auto-adapts via onSurface attribute

        Downloaded and converted verbatim from Google's official SVG —
        no hand-editing of pathData. The path geometry is exactly what
        Google publishes.

        NOTE (T19): inside the Extended FAB the icon is re-tinted by the
        style's iconTint selector (m3_fab_efab_foreground_color_selector),
        so the onSurface tint here is only a fallback — kept for
        consistency with the other in-house ic_* drawables. Replaces the
        framework @android:drawable/ic_media_play (unstyleable, drifts
        across OS versions) as part of the MD3 audit.
    -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M8 6.82v10.36c0 .79.87 1.27 1.54.84l8.14-5.18c.62-.39.62-1.29 0-1.69L9.54 5.98C8.87 5.55 8 6.03 8 6.82z" />

</vector>
