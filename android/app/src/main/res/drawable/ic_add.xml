<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorOnSurface">

    <!--
        add — official Material Icons Round from Google Fonts.
        Source: https://fonts.gstatic.com/s/i/materialiconsround/add/v1/24px.svg

        Fill color uses ?attr/colorOnSurface (theme-aware):
          - Light theme: near-black on light bg
          - Dark theme : near-white on dark bg
          - Suisei Blue + Material You: auto-adapts via onSurface attribute

        Downloaded and converted verbatim from Google's official SVG —
        no hand-editing of pathData. The path geometry is exactly what
        Google publishes. (MD3 audit T19: replaces the framework
        @android:drawable/ic_input_add — unstyleable, drifts across OS
        versions.) Inside MaterialButtons the icon is re-tinted by the
        button style's iconTint, so this tint is only a fallback.
    -->
    <path
        android:fillColor="?attr/colorOnSurface"
        android:pathData="M18 13h-5v5c0 .55-.45 1-1 1s-1-.45-1-1v-5H6c-.55 0-1-.45-1-1s.45-1 1-1h5V6c0-.55.45-1 1-1s1 .45 1 1v5h5c.55 0 1 .45 1 1s-.45 1-1 1z" />

</vector>
