<?xml version="1.0" encoding="utf-8"?>
<!--
    API 31+ (Android 12+) splash screen overrides.
    Only shows the app icon centered on black background.
    Branding text removed — it was glitched (A flipped, u cut off).
    The icon alone is sufficient for the splash screen identity.
-->
<resources>
    <style name="Theme.OTAku.Splash" parent="Theme.SplashScreen">
        <!-- Theme-aware splash background — Suisei blue primary for continuity with main UI (P1-fix C-T-01) -->
        <item name="windowSplashScreenBackground">@color/suisei_light_primary</item>
        <!-- The icon shown during splash — uses the launcher icon foreground -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <!-- Icon background color (circular mask behind the icon) -->
        <item name="windowSplashScreenIconBackgroundColor">@color/suisei_light_primary</item>
        <!-- After splash, transition to the Suisei Blue base theme (default) -->
        <item name="postSplashScreenTheme">@style/Theme.OTAku.Suisei</item>
    </style>
</resources>
