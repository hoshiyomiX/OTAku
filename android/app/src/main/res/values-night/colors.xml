<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Dark mode overrides for utility colors.
        Theme color attributes are handled by the values-night/themes.xml
        variants of Theme.OTAku and Theme.OTAku.Suisei (dark parents + dark
        palette); the surface-container ramp (T34 audit E1) lives there too.

        T34 audit E8: the dead night overrides (card_border, log_background,
        log_text, log_divider, log_info, log_debug) were removed together
        with their base values/colors.xml entries — zero references.
    -->

    <!-- Partition list text dark override (near-white on dark surface) -->
    <color name="partition_text">#E1E3E0</color>
</resources>
